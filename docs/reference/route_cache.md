# Route Cache (Legacy Path)

This page is kept for backward compatibility with older links.

Use the canonical route cache reference:

- [Route Cache Reference](./route-cache.md)

Current CLI warm/clear commands:

```bash
# Build (sharded default)
php ./webrick route:cache --cache=.route-cache --routes=routes.php

# Clear
php ./webrick route:clear --cache=.route-cache
```
